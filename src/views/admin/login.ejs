<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin — AIRPET</title>
  <link rel="stylesheet" href="/css/output.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body class="bg-[var(--ink)] min-h-screen flex items-center justify-center px-4">

  <div class="w-full max-w-sm px-4">
    <div class="text-center mb-8">
      <div class="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-primary-500 text-white mb-4 shadow-lg shadow-primary-500/30">
        <i class="fa-solid fa-shield-halved text-3xl"></i>
      </div>
      <h1 class="text-2xl font-bold text-gray-900">Painel Admin</h1>
      <p class="text-gray-500 mt-1 text-sm">Acesso restrito ao administrador</p>
    </div>

    <% if (typeof flash !== 'undefined' && flash && flash.mensagem) { %>
      <div class="mb-4 p-3 rounded-xl text-sm font-medium
        <%= flash.tipo === 'erro' ? 'bg-red-50 text-red-700 border border-red-200' : 'bg-green-50 text-emerald-700 border border-emerald-200' %>">
        <i class="fa-solid <%= flash.tipo === 'erro' ? 'fa-circle-exclamation' : 'fa-circle-check' %> mr-1"></i>
        <%= flash.mensagem %>
      </div>
    <% } %>

    <form action="<%= adminPath %>/login" method="POST" class="bg-white rounded-2xl p-6 space-y-5 shadow-xl border border-gray-200">
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700 mb-1">E-mail</label>
        <input type="email" id="email" name="email" required autofocus
               placeholder="admin@airpet.com"
               class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-gray-900 placeholder-gray-400 focus:ring-2 focus:ring-primary-500 focus:border-transparent outline-none transition-all">
      </div>

      <div>
        <label for="senha" class="block text-sm font-medium text-gray-700 mb-1">Senha</label>
        <div class="relative">
          <input type="password" id="senha" name="senha" required
                 placeholder="••••••••"
                 class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-gray-900 placeholder-gray-400 focus:ring-2 focus:ring-primary-500 focus:border-transparent outline-none transition-all pr-11">
          <button type="button" onclick="toggleSenha()"
                  class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-500 hover:text-gray-700 transition-colors">
            <i class="fa-solid fa-eye" id="eyeIcon"></i>
          </button>
        </div>
      </div>

      <button type="submit"
              class="btn-primary">
        <i class="fa-solid fa-right-to-bracket mr-2"></i>Entrar
      </button>
    </form>

    <p class="text-center text-xs text-gray-500 mt-6">
      <a href="/" class="hover:text-gray-700 transition-colors">
        <i class="fa-solid fa-arrow-left mr-1"></i> Voltar ao site
      </a>
    </p>
  </div>

  <script>
  function toggleSenha() {
    var input = document.getElementById('senha');
    var icon = document.getElementById('eyeIcon');
    if (input.type === 'password') {
      input.type = 'text';
      icon.className = 'fa-solid fa-eye-slash';
    } else {
      input.type = 'password';
      icon.className = 'fa-solid fa-eye';
    }
  }
  </script>
</body>
</html>
