<% var isLogado = typeof usuario !== 'undefined' && usuario; %>
<% var notifCount = typeof notificacoesNaoLidas !== 'undefined' ? notificacoesNaoLidas : 0; %>
<% var currentPath = typeof req !== 'undefined' ? req.path : ''; %>

<!-- Desktop Top Nav -->
<nav class="bg-white/80 backdrop-blur-xl border-b border-gray-100 sticky top-0 z-50" id="navbar">
  <div class="max-w-5xl mx-auto px-4 sm:px-6">
    <div class="flex items-center justify-between h-14">

      <a href="<%= isLogado ? '/explorar' : '/' %>" class="flex items-center gap-2.5 group">
        <span class="bg-gradient-to-br from-primary-500 to-primary-600 text-white w-8 h-8 rounded-xl flex items-center justify-center shadow-lg shadow-primary-500/20 group-hover:scale-110 transition-all duration-300">
          <i class="fa-solid fa-paw text-sm"></i>
        </span>
        <span class="text-lg font-black tracking-tight bg-gradient-to-r from-primary-500 to-primary-600 bg-clip-text text-transparent">AIRPET</span>
      </a>

      <!-- Desktop -->
      <div class="hidden md:flex items-center gap-1">
        <% if (isLogado) { %>
          <a href="/explorar" class="relative px-3.5 py-2 rounded-xl text-sm font-medium transition-all duration-200 hover:bg-primary-50 hover:text-primary-600 text-gray-500">
            <i class="fa-solid fa-compass mr-1.5"></i>Feed
          </a>
          <a href="/pets" class="px-3.5 py-2 rounded-xl text-sm font-medium transition-all duration-200 hover:bg-primary-50 hover:text-primary-600 text-gray-500">
            <i class="fa-solid fa-paw mr-1.5"></i>Meus Pets
          </a>
          <a href="/mapa" class="px-3.5 py-2 rounded-xl text-sm font-medium transition-all duration-200 hover:bg-primary-50 hover:text-primary-600 text-gray-500">
            <i class="fa-solid fa-map-location-dot mr-1.5"></i>Mapa
          </a>
          <a href="/notificacoes" class="relative px-3.5 py-2 rounded-xl text-sm font-medium transition-all duration-200 hover:bg-primary-50 hover:text-primary-600 text-gray-500">
            <i class="fa-solid fa-bell mr-1.5"></i>
            <span data-notif-badge class="absolute -top-0.5 left-[22px] min-w-[18px] h-[18px] px-1 bg-red-500 text-white text-[10px] font-bold rounded-full flex items-center justify-center animate-pulse <%= notifCount > 0 ? '' : 'hidden' %>">
              <%= notifCount > 9 ? '9+' : notifCount %>
            </span>
          </a>

          <div class="relative ml-1" id="profileDropdown">
            <button type="button" id="profileBtn"
                    class="flex items-center gap-2 p-1.5 rounded-xl hover:bg-gray-100 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-primary-500/20">
              <% if (usuario.foto_perfil) { %>
                <img src="<%= usuario.foto_perfil %>" alt="" class="w-8 h-8 rounded-full object-cover ring-2 ring-primary-100">
              <% } else { %>
                <span class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold text-white shadow-inner"
                      style="background: <%= usuario.cor_perfil || '#ec5a1c' %>">
                  <%= usuario.nome ? usuario.nome.charAt(0).toUpperCase() : 'U' %>
                </span>
              <% } %>
              <i class="fa-solid fa-chevron-down text-[10px] text-gray-400"></i>
            </button>

            <div id="profileMenu"
                 class="hidden absolute right-0 mt-2 w-56 bg-white rounded-2xl shadow-2xl shadow-black/10 ring-1 ring-black/5 py-2 z-50 opacity-0 translate-y-1 transition-all duration-200"
                 style="opacity:0; transform:translateY(4px)">
              <div class="px-4 py-3 border-b border-gray-100">
                <p class="text-sm font-bold text-gray-900 truncate"><%= usuario.nome %></p>
                <p class="text-xs text-gray-400 truncate"><%= usuario.email %></p>
              </div>
              <a href="/explorar/perfil/<%= usuario.id %>" class="flex items-center gap-3 px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50 transition-colors">
                <i class="fa-solid fa-user w-4 text-center text-gray-400"></i> Meu Perfil
              </a>
              <a href="/perfil" class="flex items-center gap-3 px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50 transition-colors">
                <i class="fa-solid fa-gear w-4 text-center text-gray-400"></i> Configurações
              </a>
              <div class="border-t border-gray-100 my-1"></div>
              <a href="/auth/logout" class="flex items-center gap-3 px-4 py-2.5 text-sm text-red-500 hover:bg-red-50 transition-colors">
                <i class="fa-solid fa-right-from-bracket w-4 text-center"></i> Sair
              </a>
            </div>
          </div>

        <% } else { %>
          <a href="/auth/login" class="px-4 py-2 rounded-xl text-sm font-medium text-gray-500 hover:text-primary-600 hover:bg-primary-50 transition-all duration-200">
            Entrar
          </a>
          <a href="/auth/registro" class="px-5 py-2 rounded-xl text-sm font-semibold text-white bg-gradient-to-r from-primary-500 to-primary-600 hover:shadow-lg hover:shadow-primary-500/25 hover:-translate-y-0.5 transition-all duration-300">
            Criar Conta
          </a>
        <% } %>
      </div>

      <!-- Mobile hamburger (for non-logged in) -->
      <% if (!isLogado) { %>
      <button type="button" id="mobileMenuBtn"
              class="md:hidden p-2 rounded-xl text-gray-500 hover:bg-gray-100 transition-colors">
        <i class="fa-solid fa-bars text-lg" id="menuIcon"></i>
      </button>
      <% } %>
    </div>
  </div>

  <!-- Mobile Menu (non-logged) -->
  <% if (!isLogado) { %>
  <div id="mobileMenu" class="hidden md:hidden border-t border-gray-100 bg-white/95 backdrop-blur-lg">
    <div class="px-4 py-4 space-y-2">
      <a href="/auth/login" class="flex items-center justify-center gap-2 px-4 py-3 rounded-xl text-sm font-medium text-gray-700 bg-gray-50 hover:bg-gray-100 transition-colors">
        <i class="fa-solid fa-right-to-bracket"></i> Entrar
      </a>
      <a href="/auth/registro" class="flex items-center justify-center gap-2 px-4 py-3 rounded-xl text-sm font-semibold text-white bg-gradient-to-r from-primary-500 to-primary-600 transition-colors">
        <i class="fa-solid fa-user-plus"></i> Criar Conta
      </a>
    </div>
  </div>
  <% } %>
</nav>

<!-- Mobile Bottom Navigation (logged in only) -->
<% if (isLogado) { %>
<nav id="bottomNav" class="md:hidden fixed bottom-0 left-0 right-0 z-50 bg-white/90 backdrop-blur-xl border-t border-gray-200/60 safe-bottom">
  <div class="flex items-center justify-around h-16 px-2 max-w-lg mx-auto">
    <a href="/explorar" class="bottom-tab flex flex-col items-center justify-center gap-0.5 w-14 py-1 rounded-2xl transition-all duration-200 text-gray-400 hover:text-primary-500">
      <i class="fa-solid fa-house text-lg"></i>
      <span class="text-[10px] font-medium">Início</span>
    </a>
    <a href="/pets" class="bottom-tab flex flex-col items-center justify-center gap-0.5 w-14 py-1 rounded-2xl transition-all duration-200 text-gray-400 hover:text-primary-500">
      <i class="fa-solid fa-paw text-lg"></i>
      <span class="text-[10px] font-medium">Pets</span>
    </a>
    <button type="button" id="fabNewPost" class="relative -mt-5 w-12 h-12 bg-gradient-to-br from-primary-500 to-primary-600 rounded-2xl flex items-center justify-center text-white shadow-lg shadow-primary-500/30 hover:shadow-xl hover:shadow-primary-500/40 hover:scale-105 active:scale-95 transition-all duration-300">
      <i class="fa-solid fa-plus text-lg"></i>
    </button>
    <a href="/notificacoes" class="bottom-tab relative flex flex-col items-center justify-center gap-0.5 w-14 py-1 rounded-2xl transition-all duration-200 text-gray-400 hover:text-primary-500">
      <i class="fa-solid fa-bell text-lg"></i>
      <span data-notif-badge class="absolute top-0 right-2 min-w-[16px] h-[16px] px-0.5 bg-red-500 text-white text-[9px] font-bold rounded-full flex items-center justify-center <%= notifCount > 0 ? '' : 'hidden' %>">
        <%= notifCount > 9 ? '9+' : notifCount %>
      </span>
      <span class="text-[10px] font-medium">Alertas</span>
    </a>
    <a href="/explorar/perfil/<%= usuario.id %>" class="bottom-tab flex flex-col items-center justify-center gap-0.5 w-14 py-1 rounded-2xl transition-all duration-200 text-gray-400 hover:text-primary-500">
      <% if (usuario.foto_perfil) { %>
        <img src="<%= usuario.foto_perfil %>" alt="" class="w-6 h-6 rounded-full object-cover ring-2 ring-transparent">
      <% } else { %>
        <span class="w-6 h-6 rounded-full flex items-center justify-center text-[10px] font-bold text-white"
              style="background: <%= usuario.cor_perfil || '#ec5a1c' %>"><%= usuario.nome ? usuario.nome.charAt(0).toUpperCase() : 'U' %></span>
      <% } %>
      <span class="text-[10px] font-medium">Perfil</span>
    </a>
  </div>
</nav>
<% } %>

<script>
(function () {
  var profileBtn = document.getElementById('profileBtn');
  var profileMenu = document.getElementById('profileMenu');
  if (profileBtn && profileMenu) {
    profileBtn.addEventListener('click', function (e) {
      e.stopPropagation();
      var open = profileMenu.classList.contains('hidden');
      profileMenu.classList.toggle('hidden');
      if (open) {
        requestAnimationFrame(function () { profileMenu.style.opacity = '1'; profileMenu.style.transform = 'translateY(0)'; });
      } else {
        profileMenu.style.opacity = '0'; profileMenu.style.transform = 'translateY(4px)';
      }
    });
    document.addEventListener('click', function () {
      profileMenu.classList.add('hidden');
      profileMenu.style.opacity = '0'; profileMenu.style.transform = 'translateY(4px)';
    });
  }
  var mobileMenuBtn = document.getElementById('mobileMenuBtn');
  var mobileMenu = document.getElementById('mobileMenu');
  var menuIcon = document.getElementById('menuIcon');
  if (mobileMenuBtn && mobileMenu) {
    mobileMenuBtn.addEventListener('click', function () {
      var open = !mobileMenu.classList.contains('hidden');
      mobileMenu.classList.toggle('hidden');
      if (menuIcon) menuIcon.className = open ? 'fa-solid fa-bars text-lg' : 'fa-solid fa-xmark text-lg';
    });
  }
})();
</script>
